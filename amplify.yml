version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - 'yarn run docs:build'
  artifacts:
    baseDirectory: books/.vuepress/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
